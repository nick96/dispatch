language: c
compiler:
  - gcc
  - clang

install:
  - pip install --user conan
  - conan config install .conan

before_script:
  - mkdir build
  - cd build
  - conan install --build missing ..
  - cmake ..

script:
  - make
  - make test
